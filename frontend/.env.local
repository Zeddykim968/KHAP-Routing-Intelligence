# Backend URL used by Vite dev server proxy fallback.
# With the proxy configured in vite.config.js you can leave this empty
# and relative /route, /facilities etc. paths will work automatically.
# Set this explicitly if you run the backend on a different host/port.
VITE_API_URL=
